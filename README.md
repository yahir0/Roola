# Roola

Mac 向けの Claude Code Skills ランチャーアプリ（Flutter Desktop / macOS）。
登録したリポジトリと Skill の組み合わせをアイコンとして並べ、ワンクリックで対象ディレクトリで Skill 実行まで到達することを目的にしています。

## 主な機能

- **アイコングリッド**: ホーム画面に登録済みエントリをアイコンで並べ、クリックで実行画面へ
- **複数 Skill の並行実行**: ホームへ戻ってもセッションは保持。アイコン右上のバッジと上部の chip 列で実行状態を一覧できます
- **エクスプローラ**: ローカルディレクトリをブラウズし、`.claude/skills/` を検知して右クリックから「登録」「即実行」「Claude Code 起動」までワンタップ
- **アプリ内 PTY ターミナル**: `xterm` ベースのフルターミナルを内蔵。承認プロンプト・矢印キー操作・ANSI 制御に対応
- **設定画面**: エントリ追加・編集・削除、アイコン画像登録、リポジトリパスのディレクトリ選択
- **外観カスタマイズ**: 透過 / 単色 / 画像背景の切り替え
- **ヘルスチェック**: `claude` CLI の存在確認と設定画面でのバナー表示

## 前提条件

- macOS（Apple Silicon / Intel どちらも）
- [`claude`](https://docs.claude.com/claude-code) CLI が PATH に通っていること
- `git` が PATH に通っていること
- Flutter SDK（バージョンは `.fvmrc` 参照 / [FVM](https://fvm.app/) 推奨）

## セットアップと起動

```bash
# 初回セットアップ（pub get + build_runner）
make setup

# 起動
make run
```

`make` を使わない場合の同等コマンド:

```bash
flutter pub get
dart run build_runner build
flutter run -d macos --dart-define-from-file=dart_defines/prod.json
```

VSCode から起動する場合は `.vscode/launch.json` の「Launch (prod)」コンフィグを使ってください。

FVM 利用時は `make run FLUTTER="fvm flutter" DART="fvm dart"` のように上書きできます。`make help` で全ターゲットを確認できます。

## 使い方

### 1. 初回起動

アプリを初めて起動すると、ホーム画面に「登録されたランチャーがまだありません」とプレースホルダーが表示されます。「設定画面を開く」ボタン、または右上の歯車アイコンから設定画面に移動します。

### 2. エントリを追加する

設定画面右上の「＋」ボタンからエントリ追加画面を開き、以下を入力します:

| 項目 | 内容 |
|---|---|
| 表示名 | アイコン下に表示される任意のラベル（例: `my-app の lint 修正`） |
| リポジトリパス | Skill を実行するローカルリポジトリの絶対パス。右側のフォルダアイコンからディレクトリピッカーで選択できます |
| Skill 名 | `claude` に引数として渡す Skill 名（例: `tdd-red`、`commit` など） |
| アイコン | 任意の画像ファイル。選択すると 512px の正方形 PNG にリサイズしてアプリのサポートディレクトリへ保存します。未指定の場合はデフォルトアイコンが使われます |

「保存」ボタンを押すと永続化され、ホーム画面のグリッドに即時反映されます。

**バリデーション:**

- 表示名・Skill 名が空 → エラーメッセージを TextField の下に表示
- リポジトリパスが存在しない → 同様にエラー
- どれか 1 つでもエラーがあると保存できません

### 3. Skill を実行する

ホーム画面のアイコンをクリックすると実行画面に遷移し、自動的に対象ディレクトリで `claude` プロセスを PTY 上で起動します。

実行画面の構成:

- **タイトル**: エントリの表示名
- **状態バッジ**: `待機中` / `起動中…` / `実行中` / `完了 (0)` / `失敗` / `キャンセル` のラベルとカラー
- **操作ボタン**:
  - 「← 戻る」（AppBar 左上）: セッションは保持したまま、起動元のタブ（ホーム or エクスプローラ）へ戻る
  - 「⏹ キャンセル」（実行中）: PTY を SIGTERM。出力履歴と終了状態のセッションは残り、再実行可能
  - 「🔄 再実行」（終了後）: 同じエントリで新しいセッションを起動
  - 「✕ 閉じる」（終了後）: セッションを完全に破棄して起動元タブへ戻る
- **ターミナルビュー**: xterm ベース。Claude Code が出す承認プロンプト・矢印キー選択 UI・ANSI 制御を解釈して描画します

### 3.x. 複数 Skill の並行実行とホーム画面

ホームに戻ってもセッション（PTY プロセス・ターミナルスクロールバック）は破棄されません。ホーム画面上部には実行中・終了済みセッションが chip として並び、タップするとそのセッションの実行画面に復帰します。各エントリアイコンの右上にも現在の状態を示す小バッジが重なって表示されます。

異なるエントリのアイコンを順次クリックすれば複数 Skill を並行に実行できます。同じエントリは 1 セッションまでで、再クリックは既存セッションへの復帰になります。**「閉じる」を押すまでセッションは残り続けます**ので、終了済みの出力履歴を見返してから明示的に破棄してください。

chip 右側の ✕ ボタンからは実行画面に入らずホームから直接セッションを破棄できます（実行中なら PTY 強制終了 → 破棄）。

### 3.y. アプリ終了時の確認

実行中・終了済みセッションが 1 件でも残っている状態でウィンドウを閉じる操作（× ボタン / Cmd+Q）を行うと、確認ダイアログ「N 件のセッションが残っています。終了するとすべての PTY が終了され、出力履歴も失われます」が表示されます。「終了する」を選ぶと全 PTY を SIGTERM して実際に終了、「キャンセル」を選ぶとアプリが継続します。セッションが残っていない場合は確認なしでそのまま閉じます。

### 4. ターミナル操作

PTY 統合のため、通常のターミナルと同じ感覚で操作できます:

- **テキスト入力 + Enter**: 改行 (`\n`) 付きで PTY に書き込まれる
- **承認応答**: `y` や `n` を入力 → そのまま `claude` の確認プロンプトに応答
- **矢印キー (↑↓←→)**: ANSI エスケープシーケンスとして PTY に送信。Claude Code の選択 UI を操作できます
- **Ctrl-C**: 0x03 を送信し、`claude` 側で SIGINT 相当の中断
- **ウィンドウリサイズ**: アプリウィンドウを変えると PTY 側の cols/rows も自動追従

### 5. エントリの編集・削除

設定画面に並んだエントリ行をタップすると編集画面が開き、既存値がプリロードされます。同じフォームで上書き保存できます。

行右端の 🗑 削除ボタンを押すと確認ダイアログが出て、「削除」を選ぶと永続化ファイルとアイコン画像から取り除かれます。

### 6. 背景・外観のカスタマイズ

設定画面上部の「外観」セクションで背景モードを切り替えられます:

- **透過**（デフォルト）: ウィンドウ背景を透明にし、デスクトップが透けて見える
- **単色**: プリセット 6 色から選択
- **画像**: ローカルの画像ファイルを選び、`<appSupport>/background.png` にコピー保存して背景にカバー表示

設定変更は即時反映され、再起動は不要です。

### 6.4. エクスプローラ

画面上部の「ホーム / エクスプローラ」タブで切り替えます。タブを切り替えてもエクスプローラの表示中ディレクトリやスクロール位置は破棄されず、すぐ戻って続きを操作できます。`.claude/skills/<name>/SKILL.md` が見つかったフォルダにはバッジが付きます。

- **ルートディレクトリ**: 初回はホームディレクトリから。AppBar の「📂↑」ボタンでフォルダピッカーから別ディレクトリに変更でき、選択した場所は次回起動時にも記憶されます
- **AppBar 左上の戻る矢印**: ルート配下に居る時にだけ表示。タップで 1 階層上に移動（ルートでは非表示）
- **フォルダタップ**: その子ディレクトリへ移動
- **フォルダ右クリック (or Control + クリック)**: アクションメニュー
  - 「このディレクトリで Claude Code を開く」: Skill 引数なしで PTY 上に `claude` 対話モードを起動 → chip 列に「ディレクトリ名 (Claude)」で並ぶ
  - **「`<skill>` を即実行」** (Skill 検知時のみ): アイコン登録なしでその場で Skill を起動 → chip 列に「ディレクトリ名 / Skill 名」で並ぶ
  - **「`<skill>` をホームに登録」** (Skill 検知時のみ): エントリ追加画面をリポジトリパス + Skill 名プリフィル状態で開く

「即実行」「Claude Code を開く」で起動した ad-hoc セッションはホームのアイコングリッドには永続追加されません。chip の ✕ で破棄するまでアプリ内に残り、続きから使えます。実行画面の AppBar 左上の戻る矢印を押すと、起動元タブ（このケースではエクスプローラ）へ戻り、開いていたディレクトリの表示状態がそのまま復元されます。

### 6.5. 動作確認用テストスキル

本物の Skill を動かすと API コストやファイル副作用が発生するため、開発時の動作確認用に副作用のないテストスキルを同梱しています。

- **リポジトリパス**: このリポジトリ直下の `tools/test-skills` を絶対パスで指定（例: `/Users/you/FlutterProjects/Roola/tools/test-skills`）
- **Skill 名**: `test-launcher-pulse`
- **挙動**: Bash で 5 秒間隔のハートビートを 12 回（約 1 分）出力するだけ。ファイル変更・ネットワーク呼び出し・Git 操作なし

このスキルを 2〜3 個並行で立ち上げると、chip 列・アイコンバッジ・キャンセル・閉じる・終了時確認ダイアログまで一通り検証できます。

### 7. `claude` コマンドのヘルスチェック

設定画面上部に `claude --version` を実行した結果のバナーが出ます。コマンドが見つからない場合は赤バナーで警告。インストールと PATH を確認してください。

## トラブルシューティング

| 症状 | 対処 |
|---|---|
| 「`claude` コマンドが見つかりません」と表示される | `which claude` で PATH を確認。`~/.zshrc` などにパスを追加した後、アプリを再起動 |
| 「リポジトリディレクトリが見つかりません」と実行画面でエラー | 設定画面で該当エントリを編集し、リポジトリパスを正しい値に更新 |
| ターミナルの描画が崩れる | ウィンドウサイズを一度変更すると再レイアウトされます |
| アプリ自体が起動しない | `flutter clean && flutter pub get && dart run build_runner build` の順で再ビルド |
| 永続化されたエントリ・設定をリセットしたい | `~/Library/Application Support/io.github.yahir0.roola/` 配下のファイルを手で削除 |
| Mac スリープから復帰したら一部 Skill が `failed` になっている | スリープ中に Anthropic API への SSE ストリーミングがタイムアウトで切断されたため。承認待ち / 完了済みのセッションは影響を受けません。該当 chip を「閉じる」で破棄して必要なら再実行してください |

## 既知の制約

- macOS 専用です（Windows / Linux サポートは現時点では行っていません）
- App Sandbox は **無効化** されています（PTY 起動と任意ディレクトリへのアクセスのため）。配布フェーズで再評価が必要です
- `riverpod_lint` / `custom_lint` は riverpod 3.x との依存解決の都合で現在保留中（詳細は `docs/adr/0007-riverpod-lint-deferral.md` を参照）
- 実 PTY 上での完了・キャンセル系のテストは `flutter test` のテストバインディングでは検証できず、`integration_test` 対象です

## 開発者向け情報

設計判断は `docs/adr/` を参照してください。アーキテクチャの詳細は `docs/architecture.md`、コーディング規約は `docs/coding-standards.md` にあります。

仕様駆動開発として [OpenSpec](https://github.com/lukasvalle/openspec) を採用しています。新しい change は `openspec new change <name>` で起こせます。

開発時の典型コマンド:

```bash
# コード生成を watch
make watch

# format → analyze → test（CI 相当）
make check

# 永続化されたエントリ・設定をリセット
make reset
```

## ライセンス

未定（公開時に設定）。
