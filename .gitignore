# macOS
.DS_Store

# Claude Code (local)
.claude/settings.local.json

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.fvm/
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds

# pubspec.lock を commit する（アプリのため、再現性を最優先）
# 何かを変えた覚えがあれば pubspec.lock を確認すること

# Coverage
coverage/

# IDE
.idea/
*.iml
.vscode/*
!.vscode/launch.json
!.vscode/settings.json
!.vscode/extensions.json

# Generated（build_runner で生成、commit する）
# *.freezed.dart
# *.g.dart

# macOS / iOS native
**/.symlinks/
**/Flutter/ephemeral/
**/Pods/
**/Pods/Pods.xcodeproj/project.xcworkspace/

# dart_defines: ローカル上書き分のみ無視
dart_defines/*.local.json

# Logs
*.log

# Editor swap files (vim 等)
*.swp
*.swo
*~

# Secrets / credentials の保険（うっかり commit 防止）。
# 実際にこの種のファイルを置く運用は今のところないが、将来事故ったとき
# のガードレールとして広めに弾いておく。
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.p8
*.mobileprovision
*.cer
secrets.*
**/credentials.json
**/google-services.json
**/GoogleService-Info.plist
